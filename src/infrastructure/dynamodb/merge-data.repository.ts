
export class MergeDataRepository {
    

    // mostrar los datos almacenados (fusionados) de ambas apis
    // async find(): Promise<MergeData[]> {
    //     const command = new ScanCommand({
    //         TableName: this.tableName
    //     })
    //     const response = await this.client.send(command);
    //     const items = response.Items || [];
    //     return items.map((d: any)=> MergeData.fromPrimitives(d));
    // }

    
}